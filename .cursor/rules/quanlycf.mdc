---
description: Quy tắc dự án QuanLyCF — chi tiết trong thư mục docs/
globs: ["**/*"]
alwaysApply: true
---

# QuanLyCF — Cursor

> **Toàn bộ tài liệu & quy tắc nằm trong thư mục `docs/`.** File này chỉ là con trỏ.

TRƯỚC KHI CODE, đọc `docs/01-QUY-TAC-CODE.md` (quy tắc) và `docs/02-KIEN-TRUC-BACKEND.md` (cấu trúc).

Cốt lõi: Vertical-slice theo Feature; Controller mỏng (logic ở Service); DTO `record`, không trả entity ra API; EF Code First (đổi schema bằng migration, KHÔNG sửa DB tay); RBAC `[Authorize(Policy = Quyens.XXX)]`; tiền `decimal`, thời gian UTC; hash BCrypt, không hardcode secret; build + test trước khi xong.
